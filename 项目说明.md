# BOSS 直聘岗位采集 · 清洗 · 筛选工具（本地运行）

> 个人学习 / 技术简历项目。纯本地运行，数据不出本机；AI 技能提取用本地大模型（Ollama）。
> 交付目录：`D:\BossJob\`

---

## 1. 一句话简介

一个本地运行的岗位数据处理工具：从 BOSS 直聘拉取公开岗位（或导入自己的 CSV）→ 把杂乱的薪资/城市/技能文本清洗成结构化字段 → 按 城市 / 薪资 / 技能 / 关键词 / HR活跃度 筛选 → 导出 CSV。核心清洗与筛选**纯 Python 标准库、零第三方依赖**。

---

## 2. 功能清单

| # | 功能 | 说明 |
|---|------|------|
| 1 | **数据加载** | 内置示例 CSV；支持上传自己的 CSV；支持「实时拉取」 |
| 2 | **薪资归一** | `8-12K`、`15-20K·13薪`、`150-200元/天`、`2-3万`、`面议` → 统一的「元/月」上下限 |
| 3 | **城市归一** | `广州·天河区·体育西路` → `广州` |
| 4 | **去重** | 按「公司 + 岗位 + 薪资 + 城市」去���，标记重复行 |
| 5 | **缺失标记** | 公司名/岗位名为空 → 标记「缺失关键信息」并高亮 |
| 6 | **技能提取（正则）** | 从 JD/列表信息里命中 Python / SQL / 爬虫 / pandas 等关键词 |
| 7 | **技能提取（AI）** | 一键调本地 Ollama（`qwen3.5:9b`）从 JD 语义抽取技能，不依赖关键词表 |
| 8 | **多条件筛选** | 城市、最低月薪、技能（多选 AND）、关键词、排除重复、仅完整记录、**HR活跃度** |
| 9 | **导出** | 一键下载清洗结果 `boss_cleaned.csv`（带 BOM，Excel 不乱码） |
| 10 | **实时拉取** | 连本地已登录真实 Chrome（CDP），拿**明文薪资**的岗位列表 |
| 11 | **备用导出** | 书签方式：在自己浏览器里点一下，自动翻页收集并导出 CSV（零自动化） |

---

## 3. 完整流程

### 路径 A：实时拉取（主流程）
```
双击 start.bat
   ├─ 1/4 装采集依赖（websocket-client / requests，仅首次）
   ├─ 2/4 启动「BOSS 专用 Chrome」→ 你登录一次（登录态持久保存）
   ├─ 3/4 启动本地大模型 Ollama
   └─ 4/4 启动网页服务 → 打开 http://localhost:8090

网页操作：
   顶部「实时拉取」面板填 关键词 / 城市 / 翻页次数 / 抓详情数
   → 点「拉取并筛选」
   → 后端调 CDP 抓取 → 清洗 → 载入表格
   → 用下方筛选条件（城市/月薪/技能/关键词/HR活跃度）过滤
   → 点「下载清洗后 CSV」
```

### 路径 B：导入自己的 CSV
```
网页点「上传 CSV」→ 选文件 → 自动清洗 → 筛选 → 导出
```

### 路径 C：书签导出（站点对自动化访问较敏感时的备选）
```
打开 BOSS导出书签.html → 把蓝色按钮拖到书签栏
→ 在自己 Chrome 里搜岗位 → 点书签 → 自动翻页收集 → 下载 CSV
→ 回工具页「上传 CSV」导入
```

---

## 4. 架构与文件结构

```
D:\BossJob\
├─ start.bat              # 唯一入口（ASCII + CRLF）
├─ boss_job.py            # 后端：HTTP 服务 + 清洗/筛选/AI/采集 API（纯标准库）
├─ index.html             # 前端：原生 HTML+JS，无 CDN
├─ fetch_boss.py          # 采集入口：调 CDP 脚本并映射成本项目字段
├─ boss_scraper\          # vendored 采集核心（eatmoreduck/boss-zhipin-scraper，MIT）
│  ├─ scripts\boss_cdp_raw.py
│  ├─ data\city_codes.json
│  ├─ chrome-profile\     # 专用 Chrome 登录态（勿提交）
│  └─ job-result\         # 原始抓取结果（勿提交）
├─ BOSS导出书签.html      # 备用：浏览器内一键导出
├─ sample_raw.csv / README.md
└─ data\latest.csv        # 最近一次采集结果
```

**接口**
| 方法 | 路径 | 作用 |
|------|------|------|
| GET | `/` | 网页 |
| GET | `/api/sample` | 返回示例 CSV |
| POST | `/api/clean` | `{csv}` → 清洗后 rows + stats |
| POST | `/api/filter` | `{rows, filters}` → 过滤结果 |
| POST | `/api/extract` | `{rows, limit}` → 调本地大模型抽技能 |
| POST | `/api/fetch` | `{keyword, city, pages, details}` → 实时拉取并清洗 |

**filters 支持**：`city`、`min_salary`、`skills[]`、`keyword`、`exclude_dup`、`only_complete`、`hr_days`

---

## 5. 关键实现细节

| 点 | 做法 |
|----|------|
| **采集方式选型** | 实测中，由程序启动的受控浏览器较容易被站点识别为自动化访问（返回 `security.html`，`code=37`），即使使用本机 Chrome 且有界面也会触发。因此改为 **基于 CDP 连接用户已登录的 Chrome**，读取页面自身请求到的搜索接口响应（不额外注入请求），薪资字段为接口明文 `salaryDesc`，不受前端字体加密影响 |
| **薪资解析** | 正则抽取数字 + 单位判定（元/天×21.75、万/月×10000、K×1000、纯数字按元），输出 `薪资下限/薪资上限/薪资单位` |
| **去重键** | `(公司, 岗位, 薪资, 城市)` 小写 —— 含城市可避免跨城同岗位被误判 |
| **HR活跃度** | 列表 API 只给粗粒度（`bossOnline`→「在线」）；细粒度「刚刚活跃/今日活跃」**只在详情页**。为求速度默认 `details=0`（不抓详情），需要时设 10~20（每条约 10-25 秒）。归一为天数：刚刚/今日=0、3日内=3、本周=7、本月=30、半年=180、未知=999 |
| **CDP 脚本调用坑** | `--output` / `--detail-output` 必须是**完整 .json 文件路径**（脚本靠 `rsplit(".")` 派生 CSV）；传目录会 `PermissionError` |
| **.bat 编码坑** | 该机控制台是 UTF-8，`.bat` 用 GBK 反而显示成 `?`，故统一 **纯 ASCII + CRLF + `chcp 65001`** |
| **服务重启** | `start.bat` 里先按 `127.0.0.1:8090` 找 LISTENING 的 PID 再 `taskkill`，保证加载最新代码 |

---

## 6. 依赖与运行

- **核心功能（清洗/筛选/网页）**：零依赖，Python 标准库即可
- **AI 技能提取**：本地 Ollama + `qwen3.5:9b`（`D:\Ollama`）
- **实时采集**：`websocket-client`、`requests`（`start.bat` 自动装）+ 本机 Chrome
- **运行**：双击 `start.bat`（或 `python boss_job.py` 后开 `http://localhost:8090`）

---

## 7. 已验证结果（2026-09-13 实测）

| 检查项 | 结果 |
|--------|------|
| 三个 py 文件编译 | 全部 OK |
| `GET /`、`/api/sample`、`/api/clean`、`/api/filter`、`/api/extract`、`/api/fetch` | 全部 200，返回合法 JSON |
| 示例数据清洗 | 19 条，重复 4、缺失 1 |
| 深圳 + 月薪≥15000 | 命中 4/19 |
| **实时拉取（真实）** | 单次抓到 **45 条** 与 **15 条**，薪资均为明文（`5-7K`、`15-30K·14薪`），清洗后 100% 有薪资 |
| 字段映射单测 | 薪资/城市/经验/学历/HR活跃天数/筛选 全部正确 |

---

## 8. 已知限制

1. **访问限制**：仍可能触发站点的访问限制（返回码 31/37）。脚本自带 12-22 秒/页 的随机延迟、单次最多 10 页；被限制时会给出可读提示。
2. **HR 活跃度**：默认不抓详情时只有「在线/空」，细粒度需 `details>0`（慢）。
3. **详情页 JD**：默认不抓，JD 由列表的 skills/标签拼成；需要完整 JD 设 `details>0`。
4. **无法在沙箱/无 GUI 环境验证 CDP 全链路**（依赖本机 Chrome + 用户登录）。

---

## 9. 合规与开源注意事项（发 GitHub 前）

- 本项目**仅供学习研究**，使用自己的账号查看公开岗位，**不抓手机号/微信/简历等隐私**，不破解验证码、不突破任何访问控制。
- **不要提交**任何抓取结果、`chrome-profile`、Cookie/登录态、城市码表以外的数据。
- `boss_scraper/` 下的 vendored 代码来自 `eatmoreduck/boss-zhipin-scraper`（MIT），**必须保留其 LICENSE 与署名**。
- README 已含免责声明，建议再加：MIT/LICENSE 文件、`.gitignore`（忽略 `boss_scraper/chrome-profile`、`job-result`、`data/_debug.*`、`__pycache__`、`_*` 临时文件）。
- 法律风险提示（非法律意见）：主要风险是**违反平台用户协议**与**批量抓取/转售数据**；本工具不破解验证码、不突破访问控制、仅供个人学习且访问频率很低，相较而言风险较低——但请自行评估，勿商用。

---

## 10. 简历项目描述（可直接用）

> BOSS 直聘公开岗位数据采集与筛选项目：使用 Python 完成公开招聘信息的结构化清洗，将薪资、城市等杂乱文本解析为统一字段，利用去重与空值处理整理为可用数据表；编写筛选逻辑按城市、薪资门槛、技能要求与 HR 活跃度过滤目标岗位，并调用本地大模型（Ollama + qwen3.5:9b）从岗位描述中语义提取技能关键词。采集环节基于 Chrome DevTools Protocol 连接本地已登录浏览器，读取页面自身的接口响应并解析为结构化字段（含薪资归一、城市归一、去重与空值处理）。核心清洗与筛选纯标准库实现，严格遵守合规要求，仅处理公开非隐私数据。
